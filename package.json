{
  "name": "gotti-reqres",
  "version": "0.1.1",
  "description": "Low level implementation of zeromq dealer and router sockets to make asynchronous req/res calls.",
  "scripts": {
    "build": "tsc",
    "test": "mocha --recursive --require ts-node/register 'test/**/*.test.ts' --exit"
  },
  "author": "visgotti",
  "license": "MIT",
  "dependencies": {
    "zeromq": "^5.1.0"
  },
  "devDependencies": {
    "@types/mocha": "^5.2.5",
    "@types/node": "^10.12.15",
    "assert": "^1.3.0",
    "mocha": "^5.1.1",
    "ts-node": "^7.0.1",
    "tslint": "^5.11.0",
    "typescript": "^3.2.2"
  }
}
